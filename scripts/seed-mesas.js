/**
 * Obsoleto: antes escribía mesas de prueba en Firestore.
 * Las mesas y el mapa viven en Postgres (Supabase); usa el editor en admin o INSERT SQL según supabase/schema.sql.
 */
console.error(
  '[seed-mesas] Este script ya no se usa. Pobla mesas/map_items desde el panel admin o SQL en Supabase.'
);
process.exit(1);
