/**
 * Capa fina sobre el motor existente: parse/serialize siguen en mapMigrations + mapSchema.
 * Aquí solo tipos y helpers para el editor React.
 */
export { parseMapDocument, serializeMapDocument } from '../mapEngine/mapMigrations.js';
export { normalizeMapDocument } from '../mapEngine/mapSchema.js';
